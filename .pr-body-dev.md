## Summary
- Align Codex API and Cursor enterprise collectors with official Admin APIs
- Add credentials guide, `modeldeck credentials print` CLI, and OAuth token persistence to secrets.yaml
- Add Home Assistant OS Supervisor add-on with Configuration UI (MQTT + per-provider credentials)
- Add `addon_bootstrap.py`, CI add-on validation, and HAOS install documentation

## Test plan
- [x] `python tests/check_test_standards.py`
- [x] `ruff check src tests tools`
- [x] `pytest -m "not integration" -p no:cacheprovider` (161 tests)
- [x] `python tools/validate_ha_addon.py`
- [ ] HAOS hardware smoke after merge (add repo URL, install add-on, verify sensors)
