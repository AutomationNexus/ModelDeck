# Local secrets and environment
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
*.pyc
*.pyo
*.egg-info/
dist/
build/
tmp/

# Runtime data (local compose bind mounts)
/config/
/data/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Built Vite dashboard (gitignored — built in Docker/CI; see frontend/README.md)
src/modeldeck/webui/static/*
!src/modeldeck/webui/static/.gitkeep

# Vite / Node
frontend/node_modules/
frontend/coverage/
frontend/dist/

# Claude Code: personal/local-only files only (never shared with the team).
# CLAUDE.md and .claude/agents|commands|settings.json are COMMITTED (dev-only,
# stripped from main by .github/dev-only-paths) -- do not ignore them here.
CLAUDE.local.md
.claude/settings.local.json

# Other AI tool configs -- not used in this repo; block if ever introduced accidentally
AGENTS.md
.cursor/
.mcp.json
.cursorrules
.aider*
.windsurf/
.copilot/
.continuedev/
.codeium/
