## Summary
- Release **v0.0.2**: provider credentials polish, official Admin API alignment, HAOS Supervisor add-on with Configuration UI
- Dual-auth collectors (subscription/cookie/personal vs api/enterprise) and weekly MQTT sensors
- End-user readiness: GHCR Docker Compose, friendly HA entity IDs, multi-arch images
- Credentials guide, `modeldeck credentials print` CLI, OAuth token persistence

## Test plan
- [x] CI green on `dev` (lint, tests 97%+, integration Mosquitto, HA add-on metadata)
- [ ] Release workflow tags `v0.0.2` and publishes `:latest` on merge
- [ ] HAOS smoke: add repo URL, install add-on, verify MQTT sensors
